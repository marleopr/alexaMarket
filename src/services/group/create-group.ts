// import API from "../../api";
// import { requestHandler } from "../../api/requestHandler";
// import { Group } from "../../types/group";

// interface CreateGroupParams {
//   name: string;
//   usersIds: string[];
// }

// export const createGroupService = requestHandler<CreateGroupParams, Group>(
//   (body) => API.post("group", body)
// );
